"use client";

import Editor from "@/components/Editor/Editor";

export default function ArticleEditor() {
  return (
    <div>
      <Editor config={{}} isEditable={true} id="editorjs" />
    </div>
  );
}
